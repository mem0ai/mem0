- 所有 API 参考页面必须遵循 `api_reference_template.mdx` 定义的结构，包含方法/路径速查表、请求头/体参数表及多语言代码示例组。
- 文档导航采用层级化分组策略，在 `docs.json` 中通过 `groups` 和 `pages` 数组明确界定功能模块（如 Core Concepts, Platform Features）。
- 使用 MDX 内置组件（如 `<Info>`, `<Warning>`, `<CodeGroup>`）增强技术内容的可读性与交互性，替代纯文本描述。