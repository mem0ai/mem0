- 采用 `Makefile` 与 `hatch` 作为核心编排层，统一管理 Python 子模块的格式化、Lint 及多版本测试环境。
- 利用 `marketplace.json` 定义插件元数据，实现集成生态（integrations）与宿主环境的声明式对接。
- 通过 `.pre-commit-config.yaml` 强制实施跨模块的代码质量门禁，确保 Python 代码在提交前符合统一的 `ruff` 与 `isort` 规范。
- 依托 `pyproject.toml` 集中管理核心依赖与可选功能组，为 SDK 及各集成组件提供一致的底层运行时契约。