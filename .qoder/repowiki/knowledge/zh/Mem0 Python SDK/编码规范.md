- 所有向量存储实现必须继承 `VectorStoreBase` 并标准化返回相似度分数（越高越相似），若底层为距离度量需进行转换。
- LLM和向量存储等组件通过 `Factory` 类创建，支持通过字符串提供商名称动态实例化对应类。
- 实体参数（user_id, agent_id等）在API调用中强制要求通过 `filters` 字典传递，禁止作为顶层关键字参数。
- 配置对象中的敏感字段（如api_key, secret）在日志记录或遥测数据传输前会被自动脱敏或移除。