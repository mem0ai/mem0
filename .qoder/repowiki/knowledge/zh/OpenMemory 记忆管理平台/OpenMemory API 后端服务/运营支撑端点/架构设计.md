- 基于 FastAPI 的 `APIRouter` 实现，划分为 config、backup、stats 三个独立路由模块。
- 配置模块（config）采用数据库持久化与默认值合并策略，支持细粒度（LLM、Embedder 等）更新并触发客户端重置。
- 备份模块（backup）实现双层导出机制：包含完整关系数据的 JSON 快照与用于跨向量库迁移的逻辑记忆流（JSONL+Gzip），导入时支持覆盖或跳过模式并处理向量重嵌入。
- 统计模块（stats）通过 SQLAlchemy 聚合查询提供用户维度的记忆与应用数量概览。