- 所有数据库模型主键统一使用 UUID 类型，并默认生成随机 UUID4。
- 所有时间戳字段（created_at, updated_at）统一使用 UTC 时间，并通过默认工厂函数自动填充。
- 模型中的保留字字段（如 metadata）通过 Column 命名参数映射为带下划线的属性名（metadata_）。
- 数据库会话在工具函数中遵循“创建-使用-关闭”模式，或在依赖注入中通过 yield 确保资源释放。