- 所有页面组件和主要功能组件均标记为 "use client"，以支持客户端交互和 Redux 状态订阅。
- 列表和详情加载状态统一使用 Skeleton 组件（如 `AppCardSkeleton`, `MemoryCardSkeleton`）进行占位渲染。
- 应用图标和名称映射逻辑统一从 `@/components/shared/source-app` 的 `constants` 对象中根据 `app.name` 获取。
- 日期格式化逻辑在多个组件中重复出现，通常使用 `new Date(...).toLocaleDateString("en-US", {...})` 模式。