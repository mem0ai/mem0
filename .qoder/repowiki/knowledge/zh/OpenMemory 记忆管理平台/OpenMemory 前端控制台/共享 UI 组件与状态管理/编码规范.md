- API Hooks 统一采用 `use[Resource]Api` 命名规范，内部封装异步请求逻辑并通过 Redux dispatch 更新全局状态。
- UI 组件广泛使用 `cva` 定义 `variants` 和 `sizes`，并结合 `cn` 工具函数实现灵活的样式组合与覆盖。
- Redux Slice 遵循 RTK 最佳实践，使用 `createSlice` 定义初始状态与 Reducers，并通过 `PayloadAction` 强类型化载荷。