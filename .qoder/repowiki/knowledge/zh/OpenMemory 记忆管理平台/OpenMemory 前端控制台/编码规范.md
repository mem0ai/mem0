- 所有业务页面均遵循 Next.js App Router 规范，使用 `page.tsx` 定义路由入口，`components/` 目录存放页面级私有组件。
- 全局状态管理采用 Redux Toolkit，按领域拆分 Slice（如 `appsSlice`, `memoriesSlice`）并在 `store/store.ts` 中统一聚合。
- UI 组件库基于 shadcn/ui 封装，原子组件统一存放于 `components/ui/`，业务复合组件存放于各功能模块的 `components/` 子目录。