- **容器化编排**：利用 `docker-compose.yml` 将 FastAPI 后端、Next.js 前端及 Qdrant 向量数据库解耦为独立服务，并通过共享网络实现通信。
- **动态基础设施注入**：`run.sh` 脚本根据环境变量动态生成 `docker-compose.yml`，支持在 Qdrant、Milvus、Weaviate 等 8 种向量存储引擎间无缝切换，并自动执行后端配置初始化。
- **统一启动契约**：通过顶层 `Makefile` 和 `run.sh` 屏蔽子模块差异，提供标准化的环境初始化（`make env`）、依赖安装及全栈启动流程。