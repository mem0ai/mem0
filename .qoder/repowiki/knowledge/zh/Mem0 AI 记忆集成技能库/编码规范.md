- 所有技能定义均遵循统一的 YAML Frontmatter 规范，包含 `name`、`description`、`trigger` 条件及 `license` 等元数据。
- 集成与测试流程强制要求使用功能开关（如 `MEM0_ENABLED`），确保在默认状态下现有系统行为不受任何影响。
- 跨技能协作通过标准化的中间产物（如 `scorecard.json`、`plan.md`）进行状态传递，避免直接修改源代码以外的全局状态。
- 文档结构保持一致性，每个子技能均包含 `README.md` 用于概览，`SKILL.md` 用于核心逻辑定义，以及 `references/` 用于详细技术参考。