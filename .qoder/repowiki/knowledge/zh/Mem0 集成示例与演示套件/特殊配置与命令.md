- 图数据库示例需安装特定依赖：`pip install "mem0ai[graph]"` 以启用 Kuzu/Neo4j 支持。
- Web 演示项目统一使用 pnpm 包管理器（`packageManager: pnpm@10.5.2`），启动命令为 `pnpm dev`。
- Chrome 插件示例需执行 `webpack` 打包生成 `dist/content.bundle.js` 后方可加载至浏览器。