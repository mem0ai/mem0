- 所有异步操作均通过 `telemetry.ts` 中的 `captureEvent` 或 `captureToolEvent` 记录埋点数据。
- 记忆作用域（project/session/global）通过 `ScopeContext` 统一解析，并转换为 Mem0 对应的 `filters` 或 `params`。
- Dream 整理功能采用文件系统锁（`mem0-dream.lock`）防止多进程并发执行，并配合状态文件记录触发频率。