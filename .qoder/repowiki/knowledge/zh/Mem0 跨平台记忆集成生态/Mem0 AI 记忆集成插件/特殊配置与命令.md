- **依赖隔离安装**：执行 `scripts/ensure_deps.sh` 会在 `$HOME/.mem0/plugin-data/venv` 创建独立虚拟环境并安装 SDK，避免污染系统环境。
- **Codex 钩子注入**：需运行 `scripts/install_codex_hooks.py` 将 Mem0 的事件处理器合并到全局 `~/.codex/hooks.json` 中。
- **环境要求**：必须设置 `MEM0_API_KEY` 环境变量以连接 `mcp.mem0.ai` 远程服务。