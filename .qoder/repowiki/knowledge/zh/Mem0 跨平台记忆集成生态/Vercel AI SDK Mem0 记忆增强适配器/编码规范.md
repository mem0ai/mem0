- 在调用链中统一合并 `config.mem0Config` 与 `settings` 作为最终记忆配置，并以 `mem0ApiKey` 优先透传。
- 对记忆相关异步操作采用 try/catch 容错：写入失败仅记录错误不中断主流程，检索失败返回空结果以保证可用性。
- 将 Vercel AI SDK 的 `LanguageModelV3Prompt` 标准化为 Mem0 API 所需的消息格式，并对多模态内容（文本/图片/PDF/MDX）进行类型分支处理。
- 通过 `withoutTrailingSlash` 规范化 `baseURL`，并在请求头中统一注入自定义 `headers` 与 API Key。
- Provider 工厂函数禁止使用 `new` 调用，检测到 `new.target` 时抛出明确错误以避免误用。