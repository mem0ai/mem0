基于 Vercel AI SDK Provider v3（`@ai-sdk/provider`、`ai`），并通过 `@ai-sdk/openai|anthropic|cohere|groq|google` 适配多模型后端；构建使用 `tsup`，测试使用 `jest` + `ts-jest`（见 `package.json`、`tsup.config.ts`、`jest.config.js`）。