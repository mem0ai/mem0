- 采用双核心架构：`src/client` 封装云端 REST API 交互，`src/oss` 实现本地化记忆处理流水线。
- `src/oss` 内部通过工厂模式（`EmbedderFactory`, `LLMFactory` 等）解耦底层组件，支持 OpenAI、Ollama 等多种提供商的动态切换。
- `src/common` 定义结构化异常体系，统一处理认证、限流及网络错误。
- `src/community` 提供 LangChain 等生态集成入口，作为可选扩展模块。