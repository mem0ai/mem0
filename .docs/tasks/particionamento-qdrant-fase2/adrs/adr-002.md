# ADR-002: Índice de inquilino (is_tenant) com promoção por shard key dedicado e cluster 3 nós

## Status

Aceito

## Data

2026-06-17

## Contexto

[ADR-001](adr-001.md) decidiu o particionamento **lógico** por projeto dentro de uma coleção compartilhada. Resta definir **como** implementar isso no Qdrant, dado o código atual:

- O provider `mem0/vector_stores/qdrant.py` cria índices de payload apenas para `user_id`, `agent_id`, `run_id`, `actor_id` (schema `keyword`), e **somente em Qdrant remoto** (`_create_filter_indexes`, linhas 163–181). O campo `project` **não é indexado**.
- O escopo por projeto é um filtro de payload simples (`{"project": project}`) montado em `mcp_server.search_memory` (linha 187) e no `compat_v3`.
- A coleção atual (`openmemory`, `get_default_memory_config`) roda single-node, sem replicação.

Sem índice, o Qdrant não estima a cardinalidade do filtro de projeto, escolhe plano de busca sub-ótimo e degrada em escala. Além disso, é preciso uma rota de escala para os poucos projetos "gigantes" sem reintroduzir busca multi-coleção (que encareceria a busca cross-project comum, conforme o PRD).

## Decisão

1. **Índice de inquilino por projeto**: criar índice de payload no campo `project` como `keyword` com `is_tenant=true`, de modo que o Qdrant **co-localize fisicamente** os pontos de cada projeto (leitura sequencial → latência plana sob filtro de projeto). O índice é criado **antes** da carga de dados na coleção nova (ver [ADR-003](adr-003.md)).
2. **Promoção por shard key dedicado**: projetos pequenos/médios permanecem no shard compartilhado. Quando um projeto cruza o **limiar de promoção**, recebe um **shard key dedicado** (custom sharding do Qdrant), localizando suas requisições em nó(s) específico(s). A promoção **não** muda o contrato de busca: a leitura continua na mesma coleção, opcionalmente com `shard_key_selector` quando a busca é escopada a um projeto promovido.
3. **Topologia inicial do cluster**: coleção compartilhada criada com `shard_number ≈ 6` e `replication_factor = 2` sobre **3 nós**. Tolera a queda de 1 nó sem perda; permite redistribuição e crescimento.

A escala custom-sharding é aplicada **sob demanda** (YAGNI): nada de shard key por projeto desde o início.

## Alternativas Consideradas

### Alternativa 1: Sharding customizado por projeto desde o início

- **Descrição**: toda escrita usa `shard_key` derivado do projeto (hash) desde o começo.
- **Prós**: localização máxima de requisições por nó; previsível para projetos grandes.
- **Contras**: exige roteamento por `shard_key` em **todas** as leituras/escritas, inclusive cross-project (que precisaria varrer todos os shard keys); muito mais setup e superfície de erro para um benefício que só os poucos projetos gigantes exigem.
- **Por que rejeitada**: viola YAGNI e penaliza a busca cross-project comum; o índice de inquilino já entrega co-localização sem roteamento explícito.

### Alternativa 2: Apenas índice de inquilino, promoção via coleção dedicada

- **Descrição**: usar `is_tenant=true` e, ao promover um gigante, movê-lo para uma **coleção separada**.
- **Prós**: isolamento físico forte do projeto promovido.
- **Contras**: reintroduz busca multi-coleção para o projeto promovido e para qualquer cross-project que o inclua — exatamente o que o ADR-001 evita.
- **Por que rejeitada**: quebra a premissa de busca cross-project barata; `shard_key` dedicado dá localização sem fragmentar a coleção.

### Alternativa 3: `replication_factor=3`

- **Descrição**: replicar todos os shards nos 3 nós.
- **Prós**: resiliência e disponibilidade de leitura máximas.
- **Contras**: ~50% mais armazenamento e RAM para o volume-alvo (centenas de milhões de vetores).
- **Por que rejeitada**: custo desproporcional; RF=2 já tolera a queda de 1 nó, suficiente para a fase.

## Consequências

### Positivas

- Filtro de projeto passa a ter plano de busca ótimo e leitura co-localizada → latência plana.
- Busca cross-project permanece barata (mesma coleção; só muda/abre o filtro).
- Rota de escala clara para gigantes (shard key) sem segunda arquitetura nem multi-coleção.
- Cluster com RF=2 entrega resiliência a falha de 1 nó.

### Negativas

- `is_tenant=true` otimiza para acesso por projeto; buscas cross-project muito amplas se beneficiam menos da co-localização (aceitável — ainda usam o índice).
- Promoção de gigante é uma operação de dados adicional (mover pontos para o shard key), feita pelo worker de migração.

### Riscos

- **Risco**: versão do `qdrant-client`/servidor não suportar `is_tenant` ou custom sharding. **Mitigação**: validar versão antes da implementação; `is_tenant` e custom sharding são suportados nas versões recentes do Qdrant usadas no projeto.
- **Risco**: criar o índice de inquilino sobre base grande é custoso. **Mitigação**: índice criado na coleção nova **antes** da carga (ADR-003), não na coleção viva.
- **Risco**: limiar de promoção mal calibrado. **Mitigação**: limiar parametrizável; alertas por tamanho de projeto (ver TechSpec, Observabilidade).

## Notas de Implementação

- Estender `Qdrant._create_filter_indexes` para incluir `project` com `models.KeywordIndexParams(type="keyword", is_tenant=True)`, e os demais campos previstos (`type` keyword, `created_at` datetime, `hash` keyword).
- Custom sharding: criar a coleção com `sharding_method=CUSTOM` quando o cluster estiver ativo; usar `create_shard_key(project)` e `shard_key_selector` na promoção.
- Promoção dispara um job no worker de migração (ADR-003) que reescreve os pontos do projeto com o `shard_key` dedicado.
- Manter o índice `keyword` simples para `project` no caminho local/single-node (onde `is_tenant`/sharding não se aplicam).

## Referências

- [ADR-001](adr-001.md) — particionamento lógico por projeto
- [ADR-003](adr-003.md) — migração blue-green
- `mem0/vector_stores/qdrant.py` (`_create_filter_indexes`, `create_col`, `search`)
- PRD: `_prd.md` (Funcionalidades P0 — Particionamento lógico e Capacidade/resiliência)
- Boas práticas de multitenancy/sharding do Qdrant (pesquisa de mercado)
