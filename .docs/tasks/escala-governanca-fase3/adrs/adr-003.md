# ADR-003: Estado `quarantined` dedicado com retenção do vetor e expurgo diferido

## Status

Aceito

## Data

2026-06-18

## Contexto

A rede de segurança da Fase 3 ([PRD](../_prd.md) e [ADR-001](adr-001.md)) exige que **toda ação destrutiva** (poda por TTL, merge de duplicatas, resolução de contradição) seja **reversível** por um período antes do expurgo definitivo. Hoje o enum `MemoryState` tem `active / paused / archived / deleted`:

- `archived` é usado pelo **arquivamento iniciado pelo usuário** (com `archived_at`).
- `deleted` é um soft-delete (`deleted_at`), e o fluxo da *skill* `dream` chega a **remover o vetor do Qdrant** ao deletar.

As memórias vivem em **dois lugares**: o **vetor + payload no Qdrant** (fonte de verdade da busca) e uma **linha espelhada no PostgreSQL** (`memories`, com `state`, `archived_at`, `deleted_at`, `metadata_`), com transições auditadas em `MemoryStatusHistory`. Para a reversão funcionar, o **vetor não pode ser removido do Qdrant** no momento da quarentena — apenas excluído da busca. Reaproveitar `archived` ou `deleted` conflitaria com semânticas já existentes (arquivamento do usuário; remoção de vetor no delete).

## Decisão

Introduzir um **novo estado `quarantined`** no enum `MemoryState`, com semântica própria de governança:

- **Entrada em quarentena:** a memória passa a `quarantined` (registrando `quarantined_at` e, na `metadata_`, o motivo e o `job_id` da governança). A transição é gravada em `MemoryStatusHistory` (`changed_by` = identidade do job de governança).
- **Retenção do vetor:** o **vetor permanece no Qdrant**; seu payload recebe `state="quarantined"` (campo de payload indexado). A busca passa a **excluir** por filtro implícito tudo que não esteja `active` — sem alterar o contrato MCP.
- **Reversão:** restaurar para `active` reverte o payload e a linha SQL; como o vetor nunca saiu, a operação é barata e completa.
- **Expurgo diferido:** um job `purge` faz o **hard-delete** (vetor no Qdrant + linha SQL marcada `deleted`/removida) somente após a **janela de quarentena** (N dias, configurável). Apenas o expurgo é irreversível.
- **Proteção de pinadas:** memórias com `metadata.pinned == true` nunca entram em quarentena.

## Alternativas Consideradas

### Alternativa 1: Reusar `archived` + motivo na metadata

- **Descrição**: Usar `archived` como quarentena, distinguindo a origem (governança vs usuário) pela metadata.
- **Prós**: Sem migração de enum.
- **Contras**: Mistura arquivamento do usuário com quarentena de governança no mesmo estado; consultas e UI passam a depender de inspeção de metadata para diferenciar; risco de o expurgo automático atingir arquivamentos do usuário.
- **Por que rejeitada**: Conflação perigosa entre uma ação do usuário e uma ação automática destrutiva.

### Alternativa 2: Reusar `deleted` (soft) com retenção do vetor

- **Descrição**: Tratar `deleted`/`deleted_at` como quarentena e mudar o comportamento para não remover o vetor até o expurgo.
- **Prós**: Reaproveita o estado existente.
- **Contras**: Altera a semântica atual de `deleted` (que hoje pode remover o vetor) e o comportamento da *skill* `dream`; difícil distinguir "deletado de verdade" de "em quarentena".
- **Por que rejeitada**: Mudar a semântica de um estado já em uso introduz regressões sutis no fluxo de delete existente.

### Alternativa 3: Remover o vetor e guardar cópia para reverter

- **Descrição**: Hard-delete imediato do vetor com backup externo para eventual restauração.
- **Prós**: Busca não precisa de filtro adicional.
- **Contras**: Reversão cara (re-inserir/re-embedar); complexidade de manter o backup consistente; risco de perda.
- **Por que rejeitada**: Reversibilidade barata é requisito central; manter o vetor e filtrar é mais simples e seguro.

## Consequências

### Positivas

- Separa claramente quarentena de governança de arquivamento do usuário e de delete real.
- Reversão barata e completa (vetor nunca sai do Qdrant até o expurgo).
- Expurgo isolado num job próprio, único ponto irreversível, fácil de auditar.

### Negativas

- Requer migração de schema (novo valor de enum + `quarantined_at`) e um índice de payload `state` no Qdrant.
- A busca passa a carregar um filtro implícito por `state="active"` — leve custo e ponto de atenção para não regredir resultados.
- Vetores em quarentena ocupam armazenamento durante a janela (custo da rede de segurança).

### Riscos

- **Memória em quarentena vazando para a busca por filtro mal aplicado.** _Mitigação:_ aplicar o filtro `state=active` num ponto único (resolvedor/camada de busca) e cobrir com testes de regressão.
- **Expurgo apagar algo ainda recuperável por janela mal dimensionada.** _Mitigação:_ janela configurável e conservadora; expurgo só lê de `quarantined` com `quarantined_at` além da janela.

## Notas de Implementação

- Alembic: adicionar `quarantined` ao enum `MemoryState` e coluna `quarantined_at` (nullable, index) em `memories`.
- Qdrant: adicionar/garantir índice de payload `state` (keyword) e injetar `state="active"` no filtro de busca (camada que monta os filtros, sem tocar o contrato MCP).
- Reutilizar `update_memory_state` (`routers/memories.py`) estendido para `quarantined`/reversão, sempre gravando `MemoryStatusHistory`.
- Expurgo usa `vector_store.delete(...)` (hard-delete) apenas no job `purge`.

## Referências

- TechSpec desta fase: `../_techspec.md`
- ADR-001 (rede de segurança e faseamento): `adr-001.md`
- Modelos: `openmemory/api/app/models.py` (`MemoryState`, `Memory`, `MemoryStatusHistory`)
- Provider: `mem0/vector_stores/qdrant.py` (`delete`, `update`, `search`)
