---
title: Moonshot AI
description: "Configure Moonshot AI (Kimi) as an LLM provider in Mem0 with API key setup and optional custom endpoint configuration."
---

To use Moonshot AI LLM models, set the `MOONSHOT_API_KEY` environment variable. You can also set `MOONSHOT_API_BASE` if you need a different API endpoint (defaults to `https://api.moonshot.cn/v1`).

## Usage

```python
import os
from mem0 import Memory

os.environ["MOONSHOT_API_KEY"] = "your-api-key"
os.environ["OPENAI_API_KEY"] = "your-api-key"  # used for embedder model

config = {
    "llm": {
        "provider": "moonshot",
        "config": {
            "model": "kimi-k3",  # default model
            "temperature": 1.0,  # kimi-k3 is a reasoning model: requires temperature=1.0, top_p=0.95
            "top_p": 0.95,
            "max_tokens": 2000
        }
    }
}

m = Memory.from_config(config)
messages = [
    {"role": "user", "content": "I'm planning to watch a movie tonight. Any recommendations?"},
    {"role": "assistant", "content": "How about thriller movies? They can be quite engaging."},
    {"role": "user", "content": "I'm not a big fan of thriller movies but I love sci-fi movies."},
    {"role": "assistant", "content": "Got it! I'll avoid thriller recommendations and suggest sci-fi movies in the future."}
]
m.add(messages, user_id="alice", metadata={"category": "movies"})
```

You can also configure the API base URL in the config:

```python
config = {
    "llm": {
        "provider": "moonshot",
        "config": {
            "model": "kimi-k3",
            "moonshot_base_url": "https://your-custom-endpoint.com",
            "api_key": "your-api-key"  # alternative to using the environment variable
        }
    }
}
```

## Config

All available parameters for the `moonshot` config are present in the [Master List of All Params in Config](../config).