node_modules/
dist/
*.tsbuildinfo
.env

# local, uncommitted e2e
*.local.mjs

# pnpm is the only supported package manager here (see integrations/AGENTS.md);
# pnpm-lock.yaml is committed. Keep foreign lockfiles out.
package-lock.json
yarn.lock
