services:
  mem0_store:
    image: qdrant/qdrant:latest
    restart: unless-stopped
    ports:
      - "6333:6333"
    volumes:
      # Qdrant persists to /qdrant/storage by default; mounting elsewhere loses data.
      - mem0_storage:/qdrant/storage