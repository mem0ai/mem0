fastapi>=0.68.0
uvicorn>=0.15.0
sqlalchemy>=1.4.0
python-dotenv>=0.19.0
alembic>=1.7.0
psycopg2-binary>=2.9.0
python-multipart>=0.0.27
urllib3>=2.7.0
fastapi-pagination>=0.12.0
mem0ai>=0.1.92
openai>=1.40.0
mcp[cli]>=1.25.4
starlette>=0.40.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
pytest-cov>=4.0.0
tenacity==9.1.2
anthropic==0.51.0
ollama==0.4.8
# Self-hosted reranking (local ONNX cross-encoder, no torch) AND BM25 sparse
# vectors for hybrid search. mem0 lists fastembed as optional, so pin it here.
fastembed>=0.3.1
# Optional: shared search cache backend (used only when REDIS_URL is set).
redis>=5.0.0
# Opt-in external reranker (RERANKER_PROVIDER=cohere): `pip install cohere`.
# Not installed by default — the default reranker is the self-hosted FastEmbed one.